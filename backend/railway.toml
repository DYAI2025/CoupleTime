[build]
builder = "NIXPACKS"

[deploy]
# Railway volume must be mounted at /data in the Railway dashboard.
# Required env var: STORAGE_PATH=/data/vibemind-sessions
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
